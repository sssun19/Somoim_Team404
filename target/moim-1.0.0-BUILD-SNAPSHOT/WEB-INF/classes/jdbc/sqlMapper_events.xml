<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="test.com.moim.events">


<!--	<insert id="E_INSERT">-->
<!--		insert into somoim(num,som_title,category,max_member,create_date)-->
<!--		values(seq_somoim.nextval,#{som_title},#{category},#{max_member},sysdate)-->
<!--	</insert>-->


	<select id="E_SELECT_ALL"
			resultType="test.com.moim.events.model.EventsVO">
		select * from events
	</select>




	<select id="E_SELECT_ONE"
			resultType="test.com.moim.events.model.EventsVO">
		select * from events where num = #{num}
	</select>


        <insert id="E_INSERT">
			INSERT INTO events (num, USER_ID, TITLE, CONTENT, SAVE_NAME, START_EVENTS, END_DATE, WINNER, EVENT_TARGET, PRESENT)
			VALUES (seq_events.nextval, #{user_id}, #{title}, #{content , jdbcType=VARCHAR}, #{save_name},  TO_DATE(#{start_events}, 'YYYY-MM-DD'), TO_DATE(#{end_date}, 'YYYY-MM-DD'),  #{winner}, #{event_target}, #{present})
        </insert>
	<!--
            <update id="b_UPDATE">
                update board set title=#{title},writer=#{writer},content=#{content}
                where wnum=#{wnum}
            </update>

            <delete id="b_DELETE">
                delete from board where wnum=#{wnum}
            </delete>
            -->

</mapper>